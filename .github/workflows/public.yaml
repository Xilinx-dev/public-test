name: test for public
on:
  pull_request:
    branches: [ master ]
    paths:
      - 'README.md'
      - '!.github/**'

jobs:
  test_job:
    runs-on: [ self-hosted ]
    steps:
    - name: my mini workflow
      run: |
        echo "this is an arbitrary change and command by public PR"
        
