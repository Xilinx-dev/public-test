name: test for public
on:
  push:
    branches: [ master ]
    paths: [ 'README.md' ]
  pull_request:
    branches: [ master ]
    paths: [ 'README.md' ]

jobs:
  test_job:
    runs-on: [ self-hosted ]
    steps:
    - name: my mini workflow
      run: |
        echo "this is an arbitrary change and command by public PR"
        
